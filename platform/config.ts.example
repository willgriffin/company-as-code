// Example configuration file in TypeScript
// Copy to config.ts and customize for your deployment
// Provides type safety and IntelliSense

import { Config } from './platform/src/config/schema';

const config: Config = {
  project: {
    name: "my-startup",
    domain: "example.com", 
    email: "admin@example.com",
    description: "GitOps infrastructure for my startup"
  },
  environments: [
    {
      name: "production",
      cluster: {
        region: "nyc3",
        nodeSize: "s-2vcpu-4gb",
        nodeCount: 3,
        minNodes: 2,
        maxNodes: 6,
        haControlPlane: true,
        version: "1.31.0-do.0"
      },
      domain: "example.com"
    }
  ]
};

export default config;