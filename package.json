{
  "name": "@startup-gitops/monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.0.0",
  "description": "Enterprise GitOps template with CDKTF, Flux, SOPS, and event-driven email architecture",
  "repository": {
    "type": "git",
    "url": "https://github.com/willgriffin/company-as-code.git"
  },
  "homepage": "https://github.com/willgriffin/company-as-code#readme",
  "scripts": {
    "platform": "bun --filter @startup-gitops/platform",
    "build": "bun run build:root && bun run -r build",
    "build:root": "tsc --noEmit",
    "typecheck": "tsc --noEmit && bun run -r typecheck",
    "test": "bun run -r test",
    "lint": "bun run -r lint && bun run lint:workflows",
    "lint:fix": "bun run -r lint:fix",
    "lint:workflows": "bash -c 'if ! command -v actionlint &> /dev/null; then echo \"Installing actionlint...\"; mkdir -p .tools && cd .tools && curl -sSL https://github.com/rhysd/actionlint/releases/download/v1.7.4/actionlint_1.7.4_linux_amd64.tar.gz | tar xz && cd .. && PATH=\".tools:$PATH\" actionlint; else actionlint; fi'",
    "format": "prettier --write . && bun run -r format",
    "format:check": "prettier --check . && bun run -r format:check",
    "dev": "bun run -r dev"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.34",
    "@types/node": "^22.15.32",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "lefthook": "^1.13.6",
    "prettier": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.3",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.0.0"
  }
}
