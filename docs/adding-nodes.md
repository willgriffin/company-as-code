# Adding Nodes

Use provider templates under `templates/nodes/` and infrastructure modules under `infrastructure/modules/`. Every node should receive Nebula identity, k3s role, storage/ingress/runner labels, and optional taints.
