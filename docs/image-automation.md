# Image Automation

Flux image reflector scans runtime images and image automation commits selected tag updates back to Git. Stateful services use semver or explicit regex policies. Avoid unbounded `latest` policies for stateful or sensitive apps.
