#!/usr/bin/env bash
set -euo pipefail

# Script to create the entire Kubernetes cluster and bootstrap GitOps
# This should be the only manual step needed to get everything running

# Find repository root
REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || echo ".")
cd "$REPO_ROOT"

echo "=== Blueprint Cluster Creation ==="
echo

# Check if .envrc.secrets exists
if [ ! -f ".envrc.secrets" ]; then
    echo "❌ .envrc.secrets not found!"
    echo "Please run the initial setup first:"
    echo "  cp .envrc.secrets.example .envrc.secrets"
    echo "  # Edit .envrc.secrets with your tokens"
    echo "  ./scripts/initial-setup"
    exit 1
fi

# Load environment
echo "📋 Loading environment..."
# shellcheck disable=SC1091
source .envrc.secrets

# Validate required environment variables
required_vars=(
    "DIGITALOCEAN_TOKEN"
    "SPACES_ACCESS_KEY_ID" 
    "SPACES_SECRET_ACCESS_KEY"
    "SOPS_AGE_PRIVATE_KEY"
    "GITHUB_TOKEN"
)

for var in "${required_vars[@]}"; do
    if [ -z "${!var:-}" ]; then
        echo "❌ Required environment variable $var is not set"
        echo "Please check your .envrc.secrets file"
        exit 1
    fi
done

echo "✅ Environment loaded successfully"
echo

# Step 1: Create Kubernetes cluster
echo "🚀 Step 1: Creating Kubernetes cluster..."
cd terraform/digitalocean

terraform init -upgrade

# Export Terraform variables for Flux
export TF_VAR_do_token="$DIGITALOCEAN_TOKEN"
export TF_VAR_spaces_access_key="$SPACES_ACCESS_KEY_ID"
export TF_VAR_spaces_secret_key="$SPACES_SECRET_ACCESS_KEY"
export TF_VAR_github_token="$GITHUB_TOKEN"
export TF_VAR_sops_age_private_key="$SOPS_AGE_PRIVATE_KEY"

# Try to import existing cluster if it exists but isn't in state
if ! terraform apply -auto-approve; then
    
    echo "⚠️  Cluster creation failed. Checking if cluster already exists..."
    
    # Check if cluster exists in DigitalOcean but not in Terraform state
    EXISTING_ID=""
    
    if command -v doctl &> /dev/null; then
        echo "🔍 Checking for existing cluster using doctl..."
        EXISTING_ID=$(doctl kubernetes cluster list --format ID,Name --no-header | grep "\scumulus$" | awk '{print $1}' || echo "")
    fi
    
    # Fallback to API if doctl not available or didn't find cluster
    if [[ -z "$EXISTING_ID" ]]; then
        echo "🔍 Checking for existing cluster using DigitalOcean API..."
        EXISTING_ID=$(curl -s -H "Authorization: Bearer $DIGITALOCEAN_TOKEN" \
            "https://api.digitalocean.com/v2/kubernetes/clusters" | \
            grep -o '"id":"[^"]*"[^}]*"name":"cumulus"' | \
            grep -o '"id":"[^"]*"' | \
            cut -d'"' -f4 || echo "")
    fi
    
    if [[ -n "$EXISTING_ID" ]]; then
        echo "🔄 Found existing cluster (ID: $EXISTING_ID). Importing into Terraform state..."
        terraform import digitalocean_kubernetes_cluster.main "$EXISTING_ID"
        echo "✅ Cluster imported successfully"
    else
        echo "❌ No existing cluster found. Creation failed for another reason."
        echo "💡 Please check the DigitalOcean console manually."
        exit 1
    fi
fi

echo "✅ Kubernetes cluster ready"
cd "$REPO_ROOT"

# Step 2: Update kubeconfig (local and GitHub)
echo "🔧 Step 2: Updating kubeconfig..."
./scripts/kubeconfig-update-local
./scripts/kubeconfig-update-repo-secret

echo "✅ Kubeconfig updated locally and synced to GitHub"

# Step 3: Deploy SOPS age key
echo "🔐 Step 3: Deploying SOPS age key..."
./scripts/sops-age-deploy

echo "✅ SOPS age key deployed"

# Step 4: Sync secrets to GitHub and generate encrypted secrets
echo "🔒 Step 4: Syncing secrets to GitHub..."
./scripts/secrets-sync-to-github

echo "🔐 Generating encrypted secrets..."
./scripts/generate-encrypted-secrets

echo "📤 Committing encrypted secrets..."
git add flux/clusters/cumulus/*/secrets.enc.yaml
git commit -m "feat: add encrypted secrets for fresh cluster deployment" || echo "No new secrets to commit"
git push

echo "✅ Encrypted secrets generated and committed"

# Step 5: Wait for initial reconciliation
echo "⏳ Step 5: Waiting for Flux to reconcile..."
echo "This may take a few minutes as infrastructure components are installed..."

# Wait up to 10 minutes for flux-system to be ready
timeout=600
elapsed=0
while [ $elapsed -lt $timeout ]; do
    if kubectl get gitrepository flux-system -n flux-system --no-headers 2>/dev/null | grep -q "True"; then
        echo "✅ Flux system is ready"
        break
    fi
    
    if [ $((elapsed % 60)) -eq 0 ]; then
        echo "⏳ Still waiting... ($((elapsed / 60))m elapsed)"
        kubectl get gitrepository,kustomizations -n flux-system 2>/dev/null | head -10 || true
    fi
    
    sleep 10
    elapsed=$((elapsed + 10))
done

if [ $elapsed -ge $timeout ]; then
    echo "⚠️  Timeout waiting for Flux. Check status manually with:"
    echo "   flux get all -n flux-system"
    echo "   kubectl get gitrepository,kustomizations -n flux-system"
    echo "   kubectl get pods -A"
else
    echo
    echo "🎉 Cluster Creation Complete!"
    echo
    echo "Your Kubernetes cluster is ready and GitOps is operational using Flux Terraform provider."
    echo
    echo "Next steps:"
    echo "• Monitor Flux: flux get all -n flux-system"
    echo "• Check GitOps sync: kubectl get gitrepository,kustomizations -n flux-system"
    echo "• Check applications: kubectl get pods -A"
    echo "• View kustomize logs: kubectl logs -n flux-system deployment/kustomize-controller"
    echo
    echo "Applications will automatically deploy as Flux reconciles."
    echo "This may take 10-15 minutes for all components to be ready."
fi