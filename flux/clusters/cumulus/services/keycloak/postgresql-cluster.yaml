apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: keycloak-pg
  namespace: keycloak
spec:
  instances: 2
  primaryUpdateStrategy: unsupervised
  
  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "256MB"
      effective_cache_size: "1GB"
      
  bootstrap:
    initdb:
      database: keycloak
      owner: keycloak
      secret:
        name: keycloak-pg-app
        
  storage:
    size: 10Gi
    storageClass: do-block-storage
    
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"
      
  backup:
    retentionPolicy: "30d"
    barmanObjectStore:
      destinationPath: "s3://{{ (datasource "config").cluster.name }}-backup/keycloak-postgres"
      s3Credentials:
        accessKeyId:
          name: keycloak-postgres-backup-credentials
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: keycloak-postgres-backup-credentials
          key: SECRET_ACCESS_KEY
      endpointURL: "https://{{ (datasource "config").cluster.region }}.digitaloceanspaces.com"
      wal:
        retention: "7d"
      data:
        retention: "30d"