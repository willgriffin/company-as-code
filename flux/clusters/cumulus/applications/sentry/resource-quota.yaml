apiVersion: v1
kind: ResourceQuota
metadata:
  name: sentry-resource-quota
  namespace: sentry
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "6"
    limits.memory: 8Gi
    persistentvolumeclaims: "10"
    requests.storage: 50Gi