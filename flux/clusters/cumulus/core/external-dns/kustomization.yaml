apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns
resources:
  - namespace.yaml
  - repository.yaml
  - secrets.enc.yaml  # Encrypted secrets work with Flux Operator SOPS support
  - release.yaml