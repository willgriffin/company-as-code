name: External Secrets Setup

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        default: 'prod'
        options:
          - prod
          - staging
          - dev
  push:
    branches: [main]
    paths:
      - 'flux/clusters/cumulus/core/external-secrets/**'
      - '.github/workflows/external-secrets-setup.yml'

env:
  KUBECONFIG_FILE: ${{ secrets.KUBECONFIG }}

jobs:
  setup-external-secrets:
    name: Setup External Secrets Infrastructure
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Skip this job if IS_TEMPLATE repository variable is set to 'true'
    if: vars.IS_TEMPLATE != 'true'
    permissions:
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
          
      - name: Setup tools
        uses: ./.github/actions/setup-tools
        with:
          tools: 'kubectl,flux,gomplate'

      - name: Setup kubectl
        run: |
          # Setup kubeconfig from secret
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > kubeconfig
          chmod 600 kubeconfig
          echo "KUBECONFIG=$(pwd)/kubeconfig" >> $GITHUB_ENV

      - name: Verify cluster connectivity
        run: |
          kubectl cluster-info
          kubectl get nodes

      - name: Process and apply external secrets configuration
        env:
          # Core infrastructure secrets
          DIGITALOCEAN_TOKEN: ${{ secrets.DIGITALOCEAN_TOKEN }}
          DO_SPACES_ACCESS_KEY: ${{ secrets.DO_SPACES_ACCESS_KEY }}
          DO_SPACES_SECRET_KEY: ${{ secrets.DO_SPACES_SECRET_KEY }}
          
          # Monitoring and observability
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          
          # Email and notifications
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          
          # Application database passwords
          KEYCLOAK_DB_PASSWORD: ${{ secrets.KEYCLOAK_DB_PASSWORD }}
          MATTERMOST_DB_PASSWORD: ${{ secrets.MATTERMOST_DB_PASSWORD }}
          NEXTCLOUD_DB_PASSWORD: ${{ secrets.NEXTCLOUD_DB_PASSWORD }}
          MAILU_PROD_DB_PASSWORD: ${{ secrets.MAILU_PROD_DB_PASSWORD }}
          POSTAL_DB_PASSWORD: ${{ secrets.POSTAL_DB_PASSWORD }}
          SENTRY_DB_PASSWORD: ${{ secrets.SENTRY_DB_PASSWORD }}
          
          # Admin passwords
          KEYCLOAK_ADMIN_PASSWORD: ${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}
          NEXTCLOUD_ADMIN_PASSWORD: ${{ secrets.NEXTCLOUD_ADMIN_PASSWORD }}
          
          # OAuth2 client IDs and secrets
          MATTERMOST_OAUTH_CLIENT_ID: ${{ secrets.MATTERMOST_OAUTH_CLIENT_ID }}
          MATTERMOST_OAUTH_CLIENT_SECRET: ${{ secrets.MATTERMOST_OAUTH_CLIENT_SECRET }}
          NEXTCLOUD_OIDC_CLIENT_ID: ${{ secrets.NEXTCLOUD_OIDC_CLIENT_ID }}
          NEXTCLOUD_OIDC_CLIENT_SECRET: ${{ secrets.NEXTCLOUD_OIDC_CLIENT_SECRET }}
          MAILU_OAUTH2_CLIENT_ID: ${{ secrets.MAILU_OAUTH2_CLIENT_ID }}
          MAILU_OAUTH2_CLIENT_SECRET: ${{ secrets.MAILU_OAUTH2_CLIENT_SECRET }}
          GRAFANA_OAUTH_CLIENT_ID: ${{ secrets.GRAFANA_OAUTH_CLIENT_ID }}
          GRAFANA_OAUTH2_CLIENT_SECRET: ${{ secrets.GRAFANA_OAUTH2_CLIENT_SECRET }}
          JAEGER_OAUTH_CLIENT_ID: ${{ secrets.JAEGER_OAUTH_CLIENT_ID }}
          JAEGER_OAUTH2_CLIENT_SECRET: ${{ secrets.JAEGER_OAUTH2_CLIENT_SECRET }}
          PROMETHEUS_OAUTH_CLIENT_ID: ${{ secrets.PROMETHEUS_OAUTH_CLIENT_ID }}
          PROMETHEUS_OAUTH2_CLIENT_SECRET: ${{ secrets.PROMETHEUS_OAUTH2_CLIENT_SECRET }}
          SENTRY_OAUTH2_CLIENT_ID: ${{ secrets.SENTRY_OAUTH2_CLIENT_ID }}
          SENTRY_OAUTH2_CLIENT_SECRET: ${{ secrets.SENTRY_OAUTH2_CLIENT_SECRET }}
          
          # OAuth2 proxy cookie secrets (should be generated 32-byte base64)
          MATTERMOST_COOKIE_SECRET: ${{ secrets.MATTERMOST_COOKIE_SECRET }}
          NEXTCLOUD_COOKIE_SECRET: ${{ secrets.NEXTCLOUD_COOKIE_SECRET }}
          MAILU_COOKIE_SECRET: ${{ secrets.MAILU_COOKIE_SECRET }}
          GRAFANA_COOKIE_SECRET: ${{ secrets.GRAFANA_COOKIE_SECRET }}
          JAEGER_COOKIE_SECRET: ${{ secrets.JAEGER_COOKIE_SECRET }}
          PROMETHEUS_COOKIE_SECRET: ${{ secrets.PROMETHEUS_COOKIE_SECRET }}
          SENTRY_COOKIE_SECRET: ${{ secrets.SENTRY_COOKIE_SECRET }}
          
          # Redis passwords
          POSTAL_REDIS_PASSWORD: ${{ secrets.POSTAL_REDIS_PASSWORD }}
          SENTRY_REDIS_PASSWORD: ${{ secrets.SENTRY_REDIS_PASSWORD }}
          
          # Application-specific secrets
          POSTAL_SECRET_KEY: ${{ secrets.POSTAL_SECRET_KEY }}
          POSTAL_RABBITMQ_PASSWORD: ${{ secrets.POSTAL_RABBITMQ_PASSWORD }}
          NEXTCLOUD_SPACES_ACCESS_KEY: ${{ secrets.NEXTCLOUD_SPACES_ACCESS_KEY }}
          NEXTCLOUD_SPACES_SECRET_KEY: ${{ secrets.NEXTCLOUD_SPACES_SECRET_KEY }}
          
          # AWS SES credentials for email
          AWS_SES_ACCESS_KEY_ID: ${{ secrets.AWS_SES_ACCESS_KEY_ID }}
          AWS_SES_SECRET_ACCESS_KEY: ${{ secrets.AWS_SES_SECRET_ACCESS_KEY }}
          SES_SMTP_USERNAME: ${{ secrets.SES_SMTP_USERNAME }}
          SES_SMTP_PASSWORD: ${{ secrets.SES_SMTP_PASSWORD }}
        run: |
          echo "🔐 Setting up External Secrets infrastructure..."
          
          # Apply external secrets operator first
          kubectl apply -k flux/clusters/cumulus/controllers/external-secrets-operator/
          
          # Wait for external secrets operator to be ready
          echo "⏳ Waiting for External Secrets Operator to be ready..."
          kubectl wait --for=condition=available --timeout=300s deployment/external-secrets -n external-secrets-system
          kubectl wait --for=condition=available --timeout=300s deployment/external-secrets-webhook -n external-secrets-system
          kubectl wait --for=condition=available --timeout=300s deployment/external-secrets-cert-controller -n external-secrets-system
          
          # Process central secrets configuration
          echo "🏗️ Processing central secrets configuration..."
          
          # Create temporary directory for processed manifests
          mkdir -p /tmp/external-secrets
          
          # Process source secrets with environment variables
          gomplate --file flux/clusters/cumulus/core/external-secrets/digitalocean-source-secrets.yaml \
                   --out /tmp/external-secrets/digitalocean-source-secrets.yaml
          
          # Apply other external secrets configuration
          kubectl apply -f flux/clusters/cumulus/core/external-secrets/digitalocean-secrets-namespace.yaml
          kubectl apply -f flux/clusters/cumulus/core/external-secrets/cluster-secret-store.yaml
          
          # Apply processed source secrets
          kubectl apply -f /tmp/external-secrets/digitalocean-source-secrets.yaml
          
          # Wait for secret stores to be ready
          echo "⏳ Waiting for secret stores to be ready..."
          kubectl wait --for=condition=Ready --timeout=120s secretstore digitalocean-core -n external-dns || true
          kubectl wait --for=condition=Ready --timeout=120s clustersecretstore digitalocean-secrets || true
          
          # Clean up
          rm -rf /tmp/external-secrets

      - name: Verify secret stores
        run: |
          echo "✅ Verifying External Secrets setup..."
          
          # Check secret stores status
          kubectl get clustersecretstore -o wide
          kubectl get secretstore -A -o wide
          
          # Check if source secrets are created
          kubectl get secrets -n digitalocean-secrets
          
          # Check External Secrets Operator status
          kubectl get pods -n external-secrets-system
          
          echo "🎉 External Secrets setup complete!"

      - name: Test external secret synchronization
        run: |
          echo "🧪 Testing ExternalSecret synchronization..."
          
          # Apply external-dns external secret
          kubectl apply -f flux/clusters/cumulus/core/external-dns/external-secret.yaml
          
          # Wait for secret to be synchronized
          echo "⏳ Waiting for external-dns secret synchronization..."
          timeout=60
          while [ $timeout -gt 0 ]; do
            if kubectl get secret external-dns-digitalocean -n external-dns >/dev/null 2>&1; then
              echo "✅ Secret external-dns-digitalocean synchronized successfully!"
              kubectl get secret external-dns-digitalocean -n external-dns -o yaml
              break
            fi
            echo "Waiting for secret synchronization... ($timeout seconds remaining)"
            sleep 5
            timeout=$((timeout - 5))
          done
          
          if [ $timeout -le 0 ]; then
            echo "❌ Secret synchronization timed out"
            kubectl describe externalsecret external-dns-digitalocean -n external-dns
            exit 1
          fi

      - name: Clean up
        run: |
          rm -f kubeconfig