# Copy to .secret.enc.yaml, replace placeholders, then encrypt with SOPS.
apiVersion: v1
kind: Secret
metadata:
  name: openobserve-collector-auth
  namespace: openobserve-collector
type: Opaque
stringData:
  OPENOBSERVE_BASIC_AUTH: CHANGE_ME_BASE64_EMAIL_COLON_PASSWORD
