apiVersion: v1
kind: Service
metadata:
  name: nebula-lighthouse
  namespace: nebula
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: nebula-lighthouse
  ports:
    - name: nebula
      port: 4242
      targetPort: nebula
      protocol: UDP
