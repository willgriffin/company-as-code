apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Official RabbitMQ Cluster Operator deployment with CRDs included. The
  # versioned URL avoids the source stack's floating `latest` reference.
  - https://github.com/rabbitmq/cluster-operator/releases/download/v2.22.5/cluster-operator.yml
