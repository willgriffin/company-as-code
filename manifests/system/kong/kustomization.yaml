apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helm-release.yaml
  - resources.yaml
  - plugins.yaml

# Dependencies managed at the Flux level
# This namespace depends on cert-manager-post-setup being ready