apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gateway-system

# Vendored Gateway API CRDs; see UPSTREAM.md for provenance and checksum.
resources:
  - gateway-api-crds.yaml
  - standard-install-v1.3.0.yaml
