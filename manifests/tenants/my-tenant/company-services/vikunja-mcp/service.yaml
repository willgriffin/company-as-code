apiVersion: v1
kind: Service
metadata:
  name: vikunja-mcp
  namespace: my-tenant-vikunja-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vikunja-mcp
  ports:
    - name: http
      port: 80
      targetPort: 3000
