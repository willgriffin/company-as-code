apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: vikunja-mcp-postgres
  namespace: my-tenant-vikunja-mcp
  labels:
    backup.company-as-code.io/target: garage
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:17
  bootstrap:
    initdb:
      database: vikunja_mcp
      owner: vikunja_mcp
  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"
      work_mem: "4MB"
      maintenance_work_mem: "64MB"
  storage:
    storageClass: TEMPLATE_FAST_STORAGE_CLASS
    size: 5Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi
  backup:
    barmanObjectStore:
      destinationPath: s3://cnpg-backups/my-tenant-vikunja-mcp-vikunja_mcp/
      endpointURL: http://garage.garage.svc.cluster.local:3900
      s3Credentials:
        accessKeyId:
          name: garage-credentials
          key: access-key-id
        secretAccessKey:
          name: garage-credentials
          key: secret-access-key
        region:
          name: garage-credentials
          key: region
      wal:
        compression: gzip
        maxParallel: 2
    retentionPolicy: "30d"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: vikunja-mcp-postgres-daily
  namespace: my-tenant-vikunja-mcp
spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: vikunja-mcp-postgres
  immediate: true
