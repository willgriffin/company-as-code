apiVersion: v1
kind: Service
metadata:
  name: bifrost
  namespace: my-tenant-ai-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bifrost
  ports:
    - name: http
      port: 80
      targetPort: 8080
