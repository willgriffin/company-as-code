apiVersion: v1
kind: Service
metadata:
  name: hindsight
  namespace: my-tenant-hindsight
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hindsight
  ports:
    - name: http
      port: 80
      targetPort: 8000
