apiVersion: v1
kind: Service
metadata:
  name: stalwart
  namespace: example-org
spec:
  selector:
    app: stalwart
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
